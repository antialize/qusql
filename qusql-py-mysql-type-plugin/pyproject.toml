[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "qusql-mysql-type-plugin"
description = "MySQL type plugin using qusql"
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python",
    "Programming Language :: Rust"
]
